{
  "name": "arch-rivals-client",
  "version": "1.0.0",
  "description": "Arch Rivals — street fighter game with architects - client side",
  "scripts": {
    "dev": "vite --port 8080",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "vitest": "^2.1.9"
  }
}
